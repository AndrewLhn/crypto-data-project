name: 'crypto_dbt'
version: '1.0.0'
config-version: 2

profile: 'crypto_dbt'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  crypto_dbt:
    +materialized: table
